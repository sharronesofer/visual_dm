apiVersion: v1
kind: Service
metadata:
  name: visual-dm-backend
  labels:
    app: visual-dm-backend
spec:
  type: ClusterIP
  selector:
    app: visual-dm-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000 