apiVersion: v1
kind: Service
metadata:
  name: visual-dm-web
  namespace: visual-dm
  labels:
    app: visual-dm
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: visual-dm
    component: web 