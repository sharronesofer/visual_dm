apiVersion: v1
kind: Service
metadata:
  name: visual-dm-redis
  namespace: visual-dm
  labels:
    app: visual-dm
    component: redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: redis
    protocol: TCP
    name: redis
  selector:
    app: visual-dm
    component: redis 